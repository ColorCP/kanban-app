---
name: vibe-coding-frontend-builder
description: 當需要從零或基於現有架構打造中大型、高互動性、多狀態流轉的前端應用或全棧產品，並希望透過嚴謹的規格驅動（Spec-Driven）與真實瀏覽器端對端驗收流程推進時使用。
---

# VibeCoding 前端應用開發 Skill

## 適用情境
- **B2B 企業後台與 SaaS 系統**：ERP、CRM、CMS、電商商品/訂單中心、多角色權限審批管理後台。
- **高互動創作與協同工具**：區塊式/富文字編輯器、Markdown 知識庫、線上白板、輕量畫布設計工具。
- **多模態數位資產管理（DAM）**：素材庫、雲盤系統、音視頻或圖片線上轉檔與裁切中心。
- **資料密集型分析與決策看板**：金融報表、營運指標儀表板、使用者旅程追蹤與視覺化圖表。
- **AI Agent 與生成式應用工作台**：Prompt 評測實驗室、RAG 知識庫配置、多步驟對話流程編排器。

---

## 核心方法論（四心法）
1. **規格先行，代碼隨後（Spec Before Syntax）**：以 PRD 與技術規格嚴密約束 AI 上下文邊界，先想清楚架構邊界再動手編寫程式碼。
2. **垂直切片，漸進交付（Vertical Slicing & Phased Iteration）**：將大功能垂直切分為低耦合 Phase，每步皆包含獨立可運行的資料流與介面鏈路。
3. **數據先行，真實驗收（Realistic Data First）**：介面開發前先注入高擬真、多邊界條件的種子資料，杜絕空白元件的虛假驗收。
4. **視覺走查，終端閉環（Browser E2E Verification）**：拒絕僅憑單元測試通過即收工，透過真實瀏覽器控制進行端對端互動與全解析度截圖走查。

---

## 步驟序列

### 步驟 1：產品需求規格化（PRD 定義）
- **目的**：從使用者與業務角度明確「要做什麼（What & Why）」，定義產品邊界、核心使用者旅程與功能價值，避免無效擴展。
- **Prompt 模板**：
  ```markdown
  請幫我新增 [需求文件名稱，如 prd.md]，規劃 [目標應用名稱，如：企業級資產管理系統] 的核心功能規格。
  該系統主要解決 [核心痛點/業務目標]，目標使用者為 [目標角色，如：行銷設計師與內容管理員]。

  請至少包含以下核心模組規劃：
  1. [核心模組 A，如：資產總覽與篩選模組]：
     - [關鍵功能 1，如：狀態分流視圖（全部/草稿/審核中/已發佈）]
     - [關鍵功能 2，如：多維度組合搜尋與自定義欄位持久化]
  2. [核心模組 B，如：內容創作與編輯模組]：
     - [關鍵功能 1，如：結構化區塊輸入與多媒體嵌入]
     - [關鍵功能 2，如：自動儲存、版本比對與防呆機制]
  3. [核心模組 C，如：資源處理與安全防護模組]：
     - [關鍵功能 1，如：客戶端轉檔與線上處理工具]
     - [關鍵功能 2，如：引用安全防刪保護機制]

  請條列化呈現，並清楚描述每個功能的「使用者操作行為」與「預期業務結果」。
  ```
- **成功判斷標準**：產出完整 Markdown 格式的 PRD，包含明確的功能範疇、使用者操作路徑與非功能性需求（如無障礙、效能），無模稜兩可的描述。

---

### 步驟 2：技術規格與資料模型轉譯（Technical Spec）
- **目的**：將 PRD 的業務語言轉譯為軟體工程架構（How），定義清晰的關聯資料庫 Schema、RESTful API 協定與非同步處理邏輯。
- **Prompt 模板**：
  ```markdown
  請根據 @[PRD 檔案路徑，如 prd.md] 的功能規劃，制定完整的技術架構與規格文件，輸出為 [技術規格名稱，如 spec.md]。

  內容必須具備嚴謹的工程落地標準，並包含：
  1. 系統架構與技術棧選型：前端 [前端框架，如 React 18 + Vite]、後端 [後端語言/框架，如 Python RESTful API]、資料庫 [資料庫類型，如 SQLite 3]。
  2. 資料庫綱要設計（Schema）：
     - 定義完整的實體資料表、各欄位型態、主外鍵約束、CHECK 約束與效能索引。
     - 針對關聯保護（如防止刪除引用中資源）設計對應的約束或檢核機制。
  3. API 規格清單：
     - 嚴格標註 HTTP Method、Path、Request Body 格式、Response 結構與錯誤狀態碼（如 400, 404, 409 Conflict）。
     - 針對高頻操作定義防抖延遲（Debounce）或併發保護策略。
  4. 前端狀態管理與容災策略：
     - 描述本地快取（如 IndexedDB / LocalStorage）與伺服器資料的同步機制。
     - 定義網路中斷或瀏覽器崩潰時的離線資料救援方案。
  ```
- **成功判斷標準**：產出技術規格文件，資料表具備關聯完整性，API 涵蓋成功與異常代碼，防抖時間與資料結構（如 JSON AST）均有明確定義。

---

### 步驟 3：模組切片與階段式任務拆解（Todo Roadmap）
- **目的**：將技術規格垂直切片為「低耦合、高內聚、可獨立測試」的多個開發階段（Phases），建立清晰的進度檢查點。
- **Prompt 模板**：
  ```markdown
  請根據 @[技術規格檔案路徑，如 spec.md]，撰寫階層式的開發任務清單 [待辦清單名稱，如 todo.md]。

  要求：
  1. 採用兩層式結構：第一層為 Phase（階段），第二層為具體可執行的子任務。
  2. 嚴格遵循由淺入深、漸進交付原則：
     - Phase 1：[核心資料模型、後端 CRUD 與基礎管理列表]
     - Phase 2：[深度核心功能，如：富互動編輯器與自動儲存引擎]
     - Phase 3：[進階模組，如：媒體中心、加工處理與引用安全防刪保護]
  3. 模組解耦要求：各 Phase 之間必須低耦合，每個 Phase 完成後都能獨立進行單元測試或 API 端點驗收，且不破壞前一階段功能。
  4. 每個子任務需附帶明確的驗收標準（Acceptance Criteria）。
  ```
- **成功判斷標準**：生成具有層次結構的 Todo 清單，標示每個任務的檔案歸屬、相依性順序與驗收條件。

---

### 步驟 4：基礎骨幹與 MVP 鏈路落地（Phase 1 開發）
- **目的**：先攻下系統骨幹，打通資料庫連線、核心 API 與基本展示列表，並建立真實測試種子資料以利後續視覺化驗證。
- **Prompt 模板**：
  ```markdown
  請使用 [後端技術，如 Python] 作為後端、[前端框架，如 React + Vite] 作為前端、[資料庫，如 SQLite] 作為 DB，根據 @[Todo 清單檔案，如 todo.md] 的 Phase 1 進行實作開發。

  請依序執行以下工作：
  1. 執行 DDL 腳本建立資料庫實體結構與必要索引。
  2. 撰寫種子資料腳本，注入至少 [數量，如 10~20 筆] 具有多種狀態、分類與維度的真實測試資料。
  3. 實作 Phase 1 的後端 API（包含分頁、多維度組合篩選與關鍵字搜尋）。
  4. 建立前端基礎頁面與列表介面（支援狀態頁籤分流、欄位顯示自定義、防抖搜尋與批量操作工具列）。
  5. 執行驗證腳本確認 Phase 1 API 運作無誤後更新 @[Todo 清單檔案]。
  ```
- **成功判斷標準**：資料庫初始化完成且塞入擬真種子資料，前後端透過 Dev Proxy 正常通訊，列表能進行分頁、篩選與狀態切換。

---

### 步驟 5：複雜互動與橫向進階模組堆疊（Phase 2+ 推進）
- **目的**：在穩定的基礎骨幹之上，橫向擴充複雜互動、非同步儲存、本地容災與跨資源引用防護等高級能力。
- **Prompt 模板**：
  ```markdown
  請繼續根據 @[Todo 清單檔案，如 todo.md] 完成 [目標階段，如：Phase 2，接著 Phase 3] 的開發。

  重點實作規範：
  1. [進階功能 A，如區塊編輯器]：
     - 支援結構化 JSON AST 區塊操作（新增、更新、上下位移、刪除）。
     - 實作 [雙軌自動儲存，如：IndexedDB 毫秒級本地救援 + 3 秒防抖雲端同步]。
     - 實作版本快照建立、歷史時間軸展開與雙欄 Visual Diff 差異比對與一鍵還原。
  2. [進階功能 B，如媒體資源中心與防護]：
     - 實作多層級虛擬目錄、拖拽上傳與二進位檔案格式安全檢驗。
     - 實作 [HTML5 Canvas 線上圖片裁切與旋轉]，支援比例鎖定與 WCAG 2.1 AA 無障礙 Alt Text 審查。
     - 實作 [引用安全防刪保護]：刪除前檢核關聯，若被引用則後端回傳 HTTP 409 Conflict，前端阻斷並彈出文章引用列表。
  3. 編寫端對端整合測試腳本驗證所有新 API，確認通過後打勾更新 @[Todo 清單檔案]。
  ```
- **成功判斷標準**：新模組順利掛載至主介面，既有功能無回退（Regression），整合測試腳本全數綠燈通過。

---

### 步驟 6：真實瀏覽器端對端驗收走查（Browser Control E2E Verification）
- **目的**：跳脫終端機純代碼與單元測試，透過原生瀏覽器自動化模擬終端使用者真實行為，進行全系統視覺與互動走查。
- **Prompt 模板**：
  ```markdown
  請透過 Browser Control（瀏覽器自動化控制）幫我測試目前系統的所有頁面與核心功能。

  測試必須覆蓋以下關鍵情境，並在關鍵狀態擷取高解析度截圖：
  1. 前台網站與深淺色主題即時切換。
  2. [Phase 1 管理列表]：測試狀態分流頁籤點擊、關鍵字防抖搜尋、自定義欄位選單切換、以及多選批量操作工具列彈出。
  3. [Phase 2 編輯器]：測試新增區塊、編輯內文、等待防抖自動儲存狀態燈轉綠（🟢）、建立版本快照點、並開啟 Visual Diff 差異比對視窗。
  4. [Phase 3 資源庫與防護]：切換虛擬目錄、開啟 Canvas 裁切工具測試比例與旋轉，以及嘗試刪除正被引用的素材以觸發 [HTTP 409 引用安全防護彈窗]。

  完成後請整理驗收成果對照表，並將截圖嵌入說明文件回報。
  ```
- **成功判斷標準**：自動化腳本順利跑完所有互動流程，無 Uncaught Exception 或逾時卡死，生成完整驗收截圖與測試對照表。

---

## 常見問題處理（Troubleshooting）

### 問題 1：上下文退化與程式碼覆寫回退（Context Drift & Code Regression）
- **現象**：在進行 Phase 2 或 Phase 3 開發時，AI 為了方便直接覆寫原有檔案，導致 Phase 1 的篩選、樣式或核心功能被洗掉。
- **根因**：大型語言模型在處理超長程式碼檔案時，容易遺漏非關聯區塊，採用暴力整檔覆寫。
- **解法**：
  1. **局部區塊替換**：嚴格要求使用精確行號的局部修改工具（如 `replace_file_content`），禁止大範圍重寫無關邏輯。
  2. **檔案職責物理隔離**：將大型視圖拆解為獨立子元件（如將編輯器、媒體庫、裁切器獨立為單獨 `.jsx` 檔案），降低單一檔案複雜度。
  3. **自動化回歸檢驗**：在交付每個 Phase 時，執行累積測試腳本（包含前幾階段的 API 測試），確保既有功能無損。

### 問題 2：非同步競爭與高頻自動儲存崩潰（Race Condition & Autosave Storm）
- **現象**：使用者快速打字時，後端收到幾十次無效請求，甚至造成資料庫鎖定（Database Locked）或版本錯亂。
- **根因**：直接在 `onChange` 事件中發起 API 呼叫，缺乏防抖機制與請求序列化。
- **解法**：
  1. **雙層防抖管線**：
     - 本地軌道：每次擊鍵以 300ms 防抖寫入瀏覽器 `IndexedDB`，確保不掉字。
     - 雲端軌道：以 3,000ms（3 秒）防抖發送 `PATCH` 請求至後端。
  2. **狀態指示燈防呆**：提供 `雲端儲存中... ⏳`、`已儲存於 XX:XX 🟢` 與 `離線保護中 🟡` 三態反饋，並在請求中帶入版本時間戳進行樂觀鎖檢驗。

### 問題 3：引用資源被誤刪造成線上破圖（404 Broken Asset Crisis）
- **現象**：使用者在資源庫中刪除了某張圖片，導致線上多篇文章的封面或內文圖片全部變成破圖（404）。
- **根因**：刪除操作只做本機檔案的 `rm`，未進行全局關聯依賴性檢核。
- **解法**：
  1. **後端 HTTP 409 Conflict 攔截**：在 `DELETE /api/media/:id` 時，後端先執行反向查詢（`SELECT FROM articles WHERE featured_image_id = ? OR content LIKE ?`），若有引用直接阻斷，回傳 HTTP 409 與被引用的文章清單。
  2. **前端攔截警示彈窗**：前端收到 409 後，阻斷預設行為並彈出「🛑 引用安全保護」視窗，列出所有引用文章連結，提示使用者必須先替換文章圖片才能刪除。

### 問題 4：無頭瀏覽器測試時原生對話框卡死（Browser Dialog Deadlock）
- **現象**：執行 Browser Control 自動化測試時，腳本卡在 `page.waitForSelector` 並在 30 秒後逾時報錯。
- **根因**：前端元件觸發了瀏覽器原生 `window.confirm()` 或 `window.alert()`（例如確認刪除或提示成功），無頭瀏覽器（Headless Chrome）若無事件監聽器會無限期等待使用者點擊。
- **解法**：
  在自動化測試腳本啟動之初，必須掛載原生事件監聽處理：
  ```javascript
  page.on('dialog', async dialog => {
    console.log(`[Auto-Handled Dialog] ${dialog.type()}: ${dialog.message()}`);
    await dialog.accept(); // 自動同意 confirm 與 alert
  });
  ```
